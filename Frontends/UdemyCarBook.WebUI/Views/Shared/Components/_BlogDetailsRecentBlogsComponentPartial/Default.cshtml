@model List<ResultLast3BlogsWithAuthors>
<div class="sidebar-box ftco-animate">
    <h3>Son Bloglar</h3>
    @foreach (var item in Model)
    {
        <div class="block-21 mb-4 d-flex">
            <a class="blog-img mr-4" style="background-image: url(/carbook-master/images/image_1.jpg);"></a>
            <div class="text">
                <h3 class="heading"><p href="#">@item.Title</p></h3>
                <div class="meta">
                    <div><p href="#"><span class="icon-calendar"></span> @item.CreatedDate.ToShortDateString()</p></div>
                    <div><p href="#"><span class="icon-person"></span> @item.AuthorName</p></div>
                    <div><p href="#"><span class="icon-chat"></span> 0</p></div>
                </div>
            </div>
        </div>
    }
</div>